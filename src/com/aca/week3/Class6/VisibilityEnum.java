package com.aca.week3.Class6;

// instead of class - enum
public enum VisibilityEnum {
    VISIBLE,  // only names
    INVISIBLE
    // created in heap and not modified
    /*
    these are objects in heap => constructors will be created (default)
     */

//    VisibilityEnum() {
//        super("VISIBLE", 0);
        // name , ordinal
//    }

}
